{
  "name": "cartographer",
  "private": true,
  "workspaces": [
    "packages/block-palettes",
    "packages/generation",
    "packages/litematica",
    "packages/pixels",
    "interface"
  ],
  "scripts": {
    "build:packages": "npm run build -w packages/pixels && npm run build -w packages/block-palettes -w packages/litematica && npm run build -w packages/generation",
    "build:interface": "npm run build -w interface",
    "build": "npm run build:packages && npm run build:interface",
    "postinstall": "node -e \"try{require('fs').copyFileSync('./scripts/pre-commit.sh','./.git/hooks/pre-commit')}catch(e){}\"",
    "start": "npm run build:packages && npm run build:interface && npm run start -w interface",
    "watch": "npm run build:packages && npm run watch -w interface",
    "test": "vitest"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "vitest": "^0.25.7",
    "@types/node": "^18.11.12",
    "prettier": "^2.8.1",
    "typescript": "^4.9.4"
  }
}